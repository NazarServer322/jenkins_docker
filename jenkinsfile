pipeline {
    agent any

    options{
        buildDiscared(LogRotator(numToKeepStr: '10', artifactNumToKeepStr '10' ))
        timestamps()
    }

    stages {
        stage('create docker image') {
            steps {
                dir('')
                sh 'docker build .'
                echo 'Hello World'
            }
        }
    }

